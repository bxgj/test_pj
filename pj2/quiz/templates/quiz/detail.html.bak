{% load bootstrap %}
<form>
    <legend>My Form</legend>
    {% csrf_token %}
	{{ qf|bootstrap_horizontal:'col-lg-4' }}
    <div class="form-actions">
      <a href="#back" class="btn">Go back</a>
      <button type="submit" class="btn btn-primary">Save changes</button>
    </div>
</form>
